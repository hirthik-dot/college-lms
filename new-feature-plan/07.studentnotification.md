Update the existing Student portal to support Course Plan material notifications.
Do NOT create new pages — update existing ones.

CHANGES NEEDED:

1. UPDATE: client/src/components/layout/Navbar.jsx
   - The notification bell icon already exists
   - Add a React Query call: GET /api/student/notifications/materials
   - Poll every 60 seconds using refetchInterval: 60000
   - Show red badge with count of unread notifications
   - Clicking the bell opens a dropdown panel:
     Each notification shows:
     - Blue document icon
     - 'New material: [topic_name]' (bold)
     - Subject name (grey small text)
     - 'X minutes ago' timestamp
     - Unread ones have a blue left border
   - Clicking a notification:
     - PUT /api/student/notifications/:id/read (mark as read)
     - Navigate to /student/content?subjectId=[id]&topicId=[id]
   - 'Mark all as read' button at bottom of dropdown
   - If no notifications: 'No new materials' empty state

2. UPDATE: client/src/pages/student/Content.jsx
   - Already shows course content grouped by subject
   - Now also fetch GET /api/student/materials/:subjectId
   - Mix these with existing content — show them in a new section:
     'Released by Faculty' section (separate from manually uploaded content)
   - Group by topic name
   - Each topic group shows: topic name as sub-header, then files below
   - Add NEW badge (blue pill) on materials released within last 24 hours
   - NEW badge disappears after 24 hours automatically (check released_at)

3. UPDATE: client/src/pages/student/Dashboard.jsx
   - Add a new card: 'Recent Materials'
   - Shows last 3 newly released materials
   - Each: file icon + topic name + subject + 'X hours ago'
   - 'View All' link goes to /student/content

All changes must be backwards compatible.
Use React Query for all new data fetching.
Generate the complete updated files.
