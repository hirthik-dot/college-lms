Wire up all the Course Plan Automation feature into the existing LMS.

1. UPDATE server/server.js:
   Import and mount coursePlan routes:
   const coursePlanRoutes = require('./src/routes/coursePlan');
   app.use('/api/faculty/course-plan', authenticate, requireRole('staff'), coursePlanRoutes.faculty);
   app.use('/api/hod/course-plan', authenticate, requireRole('hod'), coursePlanRoutes.hod);
   app.use('/api/student/materials', authenticate, requireRole('student'), coursePlanRoutes.student);
   app.use('/api/student/notifications', authenticate, requireRole('student'), coursePlanRoutes.notifications);

   Also serve uploaded files statically:
   app.use('/uploads', express.static(path.join(__dirname, 'uploads')));
   Ensure these upload subdirectories exist on startup:
   uploads/course-plans/
   uploads/proof-images/
   uploads/topic-materials/

2. UPDATE client/src/App.jsx:
   Add new faculty routes:
   /staff/course-plan → CoursePlanManager
   /staff/course-plan/submit/:topicId → SubmitHourReport

   Add new HOD route:
   /hod/course-plan-reports → CoursePlanReports

3. UPDATE client/src/components/layout/Sidebar.jsx:
   In the staff navigation section, add after 'Assignments':
   { path: '/staff/course-plan', label: 'Course Plan', icon: FileText }

   In the HOD navigation section, add after 'Attendance Reports':
   { path: '/hod/course-plan-reports', label: 'Course Plan Reports', icon: ClipboardList }

4. UPDATE server/src/config/schema.sql:
   Add a comment at the bottom: '-- Run migrations/001_course_plan.sql for Course Plan feature'

5. CREATE server/src/utils/emailService.js:
   - Uses nodemailer
   - Reads SMTP config from .env: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM
   - Function: sendMaterialReleasedEmail(studentEmail, studentName, facultyName,
     topicName, subjectName, materialCount, loginUrl)
   - HTML email template with college name, topic info, login button
   - Text fallback for email clients that don't support HTML
   - Handles errors gracefully — logs failures but does not crash the app

6. UPDATE server/.env.example:
   Add: SMTP_HOST=smtp.gmail.com
        SMTP_PORT=587
        SMTP_USER=your-college-email@gmail.com
        SMTP_PASS=your-app-password
        SMTP_FROM=noreply@yourcollege.edu
        APP_URL=http://192.168.1.100
        WHATSAPP_ENABLED=false

Generate all updated files completely.
